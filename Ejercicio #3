//Primera parte:
//Crear una función con tres parámetros que sean números que se suman entre sí.
//Llamar a la función en el main y darle valores.

public class Main {
    public static void main(String[] args) {
        //Crear un objeto miCoche en el main y añadirle una puerta.
           Coche miCoche = new Coche();
           miCoche.AlmacenarPuerta();
           miCoche.AlmacenarPuerta();
           miCoche.AlmacenarPuerta();
           System.out.println("El total de puertas almacenadas es = " + miCoche.puertas);

        int resultado = suma(20, 30,40);

        System.out.println("El total de la suma a, b, c es = " + resultado);
    }
    public static int suma(int a, int b, int c){
        return a + b + c;
    }

//Segunda parte:
//Crear una clase coche.
static class Coche {
    //Dentro de la clase coche, una variable numérica que almacene el número de puertas que tiene.
        public int puertas = 0;
    //Una función que incremente el número de puertas que tiene el coche.
        public void AlmacenarPuerta() {
            this.puertas++;
        }

//Mostrar el número de puertas que tiene el objeto.
    }
}
